import 'package:flutter/material.dart';

class DeviceLookupRoot extends StatelessWidget {
  const DeviceLookupRoot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

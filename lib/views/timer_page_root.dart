import 'package:flutter/material.dart';

class TimerPageRoot extends StatelessWidget {
  const TimerPageRoot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("1")));
  }
}
